@model eshop_MVC.Models.CouponCreateModel

@{
    ViewData["Title"] = "Kupon Oluştur";
}

<h2>Kupon Oluştur</h2>

<form asp-action="CreateCoupon" method="post">
    @Html.AntiForgeryToken()

    <div class="form-group">
        <label asp-for="Code" class="control-label">Kupon Kodu</label>
        <input asp-for="Code" class="form-control" />
        <span asp-validation-for="Code" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="DiscountPercentage" class="control-label">İndirim Yüzdesi (%)</label>
        <input asp-for="DiscountPercentage" type="number" class="form-control" min="1" max="100" step="1" placeholder="İndirim yüzdesini giriniz" />
        <span asp-validation-for="DiscountPercentage" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Kupon Oluştur</button>
    <a asp-action="Index" class="btn btn-secondary">İptal</a>
</form>


